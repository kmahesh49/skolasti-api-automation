// FeatureFlag API Payloads
// Admin API - Feature flags and subscription management

module.exports = {
  // Most endpoints are GET with no payload
  // invalidatecache is POST but deprecated and takes no body
  emptyPayload: {}
};
